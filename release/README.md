# Dawn Launcher - 重构版 (Rust + Slint)

> **v0.2 更新**
>
> 已加入基本的增删改功能：工具栏「+ 添加项目」按钮、左侧栏「+ 新建分类」按钮、
> 项目右键删除、分类右键重命名/删除。可通过文件选择对话框添加文件/文件夹，
> 也可拖拽文件 URL 到窗口添加项目（拖拽功能受 Slint 1.5 限制，详见下文）。

## 技术栈

- **Rust** + **Slint 1.18** (声明式 UI，兼容 1.18 API)
- **rusqlite** (SQLite 数据库，与原项目表结构兼容)
- **windows-rs** (Win32 API 调用)
- 交叉编译: Linux → Windows (mingw-w64)

## 已实现功能 (v0.2)

| 功能 | 状态 |
| --- | --- |
| 主窗口 (标题栏 + 关闭按钮) | ✅ |
| 分类列表 (左侧栏) | ✅ |
| 项目列表 (右侧网格) | ✅ |
| 点击分类切换项目列表 | ✅ |
| 单击项目选中 / 双击打开 | ✅ |
| 双击打开 (文件/文件夹/网址/.lnk 快捷方式) | ✅ |
| 项目图标显示 (PNG Base64) | ✅ |
| 无效项目置灰 (路径不存在) | ✅ |
| 便携版数据存储 (程序同级 `data/` 目录) | ✅ |
| **工具栏「+ 添加项目」按钮 (弹出文件选择对话框)** | ✅ v0.2 新增 |
| **左侧栏「+ 新建分类」按钮** | ✅ v0.2 新增 |
| **项目右键 → 删除** | ✅ v0.2 新增 |
| **分类右键 → 重命名 / 删除** | ✅ v0.2 新增 |
| **拖拽文件 URL 添加项目 (回调已就绪)** | ⚠️ 部分支持 |
| 自动识别文件类型 (文件/文件夹/网址/快捷方式) | ✅ v0.2 新增 |

## 未实现功能 (后续迭代)

- ❌ 编辑项目（修改名称/图标/参数）
- ❌ 项目/分类排序拖拽
- ❌ 关联文件夹分类 / 聚合分类
- ❌ 设置窗口 (主题/外观/快捷键等)
- ❌ 快速搜索窗口
- ❌ 自定义主题 / 透明窗口 / 背景图
- ❌ 多语言切换
- ❌ 全局快捷键
- ❌ 系统托盘
- ❌ 鼠标 HOOK / 边缘吸附 / 失焦隐藏
- ❌ 资源管理器右键菜单 (COM IContextMenu)
- ❌ Appx 应用扫描
- ❌ 开始菜单扫描
- ❌ 网址信息一键获取
- ❌ 数据备份与恢复
- ❌ 开机自启 / 单实例锁

## 使用方法

### 1. 启动

直接双击 `dawn-launcher.exe` 即可运行。

数据存储在 exe 同级的 `data/` 目录下：
- `data/dawn-launcher.db` (SQLite 数据库)

### 2. 添加项目（三种方式）

**方式 A：通过文件选择对话框**
1. 点击右上角「+ 添加项目」按钮
2. 在弹出的对话框中选择文件/快捷方式
3. 项目会自动添加到当前分类，名称取自文件名，图标自动提取

**方式 B：拖拽文件 URL**
- 将文件资源管理器中的文件拖入窗口右侧区域
- ⚠️ 注意：Slint 1.5 的拖拽支持需要 winit 后端配合，当前版本可能不触发，
  如拖拽无响应，请使用方式 A

**方式 C：从原 Electron 版数据库导入**
1. 找到原版数据库（通常在 `%APPDATA%\Dawn Launcher\`）
2. 复制为 `data/dawn-launcher.db` 覆盖即可

### 3. 删除/重命名

- 项目右键 → 弹出确认框 → 确认删除
- 分类右键 → 选择「重命名」或「删除」

## 体积对比

| 版本 | 安装包体积 | 安装后体积 |
| --- | --- | --- |
| 原 Electron 版 | ~80 MB | ~180 MB |
| **Rust + Slint 重构版** | **~10 MB** | **~10 MB** |

体积减少 **95%**，且**无任何运行时依赖** (无需 WebView2 / Node.js / .NET)。

## 已知限制

1. **图标提取**：当前使用 PowerShell 间接提取图标，速度较慢。
   首次添加大量项目时可能略有卡顿，后续将改用原生 `IShellItemImageFactory`。

2. **拖拽支持**：Slint 1.5 的窗口级拖拽事件尚未完整启用。
   当前 `item-dropped` 回调已就绪，但需要 winit 端的事件桥接才能触发。
   临时方案：使用「+ 添加项目」按钮添加。

3. **对话框**：确认框使用原生 MessageBox，输入框/动作选择框使用 PowerShell 的
   `Microsoft.VisualBasic.Interaction.InputBox`，在精简版 Windows 上可能略有延迟。

4. **UI 美观度**：当前 UI 较为朴素，未实现透明窗口、自定义主题、动画等。

## 反馈方式

测试时如遇问题，请记录：
1. 操作系统版本 (Win10/Win11, 是否精简版)
2. 复现步骤
3. 现象描述 (启动失败/崩溃/无显示/功能异常等)
4. 同级 `data/` 目录是否存在 `dawn-launcher.db` 文件

## 编译信息

- 编译时间: 2026-09-20
- Rust 版本: rustc 1.92.0
- Slint 版本: 1.18.0
- Target: x86_64-pc-windows-gnu
- Profile: release (opt-level=z, lto=true, strip=true)

## 源代码

完整源代码与构建说明见仓库根目录的 Rust 源码 (`src/`、`ui/`、`Cargo.toml`)。
原 Electron 版源码备份在 `legacy-electron/` 目录。
